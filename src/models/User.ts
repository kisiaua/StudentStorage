export type UserFormRegister = {
  firstName: string;
  lastName: string;
  email: string;
  password: string;
  role: string;
};

export type UserFormLogin = {
  email: string;
  password: string;
};
